# ChatGPT Snippet Workspace

## Local development

1. Install dependencies: `pnpm install`
2. Copy envs: `cp .env.example .env`
3. Generate Prisma client: `pnpm prisma generate`
4. Run migrations: `pnpm prisma:migrate`
5. Start the app: `pnpm dev`
6. Run tests: `pnpm test && pnpm test:e2e`
